/** Environment variable: Teachable Admin API base URL (http(s), required). */
export const TEACHABLE_ADMIN_BASE_URL_ENV = "TEACHABLE_ADMIN_BASE_URL";

/**
 * Environment variable: value for the X-Consumer-Custom-ID header on every request (required).
 */
export const TEACHABLE_ADMIN_CONSUMER_CUSTOM_ID_ENV = "TEACHABLE_ADMIN_CONSUMER_CUSTOM_ID";

/** Header required by the Teachable Admin MVP integration. */
export const TEACHABLE_ADMIN_CONSUMER_CUSTOM_ID_HEADER = "X-Consumer-Custom-ID";
